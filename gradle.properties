# Gradle Properties
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod Properties
mod_version=1.0.0
maven_group=com.latencyviewer
archives_base_name=fabric-latency-viewer

# Fabric Loader (shared across versions)
loader_version=0.16.10
